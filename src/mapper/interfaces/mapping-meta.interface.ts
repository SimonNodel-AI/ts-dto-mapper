import {
  MappingInfo
} from './mapping-info.interface';


/**
 * Mapping metadata property interface that is added to mapped instances
 *
 * @export
 * @interface MappingMeta
 */
export interface MappingMeta {
  _mappingMeta: MappingInfo;
}
